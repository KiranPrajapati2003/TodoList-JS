<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.6.0/css/all.min.css">
    <title>Document</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            width: 100%;
            height: 100%;
            display: flex;
            justify-content: center;
        }
        .main {
            margin-top: 100px;
            width: 400px;
            padding: 20px;
            border: 2px solid black;
            background-color: #99cbdec2;
        }
        form {
            width: 100%;
        }
        .forms {
            position: relative;
            margin-bottom: 15px;
        }
        input {
            width: 90%;
            padding: 5px 7px;
            font-size: 18px;
            border: 2px solid black;
        }
        .btn {
            position: absolute;
            top: 0;
            right: -8px;
        }
        button{
            padding: 5px 7px;
            font-size: 18px;
            background-color: black;
            color: white;
        }

        ul li {
            display: flex;
            justify-content: space-between;
        }

        li {
            background-color: black;
            font-weight: 600;
            color: white;
            font-size: 18px;
            padding: 5px 7px;
            margin-bottom: 10px;
        }
        li i{
           color: white;
        }
    </style>
</head>

<body>
    <div class="main">
        <form action="/insertdata" method="post">

            <div class="forms">
                <div class="inputs">
                    <input type="text" name="todo" class="input" placeholder="Enter Task">
                    <input type="hidden" name="id" value="<%= Date.now()%>">
                </div>
                <div class="btn">
                    <button type="submit">Add</button>
                </div>
            </div>

        </form>
        <ul>
            <% todoList.map((li)=>{ %>
                <li>
                    <%= li.todo %>
                    <a href="/deleteData/<%= li.id %>"><i class="fa-solid fa-trash-can"></i></a>
                </li>

                <% }) %>
        </ul>
    </div>
 
</body>

</html>